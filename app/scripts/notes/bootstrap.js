var da = window.da || {};
//da.notes={};
da["notes"]["bootstrap"] = {
    
    title: "Bootstrap Notes",
    body: "Data attributes.",
    bodyTwo: "Nearly all Bootstrap plugins can be enabled and configured through HTML alone with data attributes (our preferred way of using JavaScript functionality). Be sure to only use one set of data attributes on a single element (e.g., you cannot trigger a tooltip and modal from the same button.)."
    };